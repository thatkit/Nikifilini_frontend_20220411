@import '../../../common.styl'

.screenWrapper
  height 100vh
  width calc(100% - 84px)
  padding 28px
  display block
  position relative
  bottom 100%
  left 84px
  overflow scroll
  coolScrollbar()

.screen
  border-radius 4px
  background white
  coolScrollbar()

.items
  display block
